package com.princess.royalscepter.data.api

object ApiConfig {
    const val DEFAULT_BASE_URL = "http://127.0.0.1:8000"

    // Local development placeholders. Inject a bearer token or session token from
    // your debug configuration instead of committing real credentials here.
    const val DEFAULT_BEARER_TOKEN = ""
    const val DEFAULT_SESSION_TOKEN = ""
}
